import { registerRootComponent } from 'expo';

import App from './App';

// Điểm bắt đầu (entry point) của ứng dụng Expo.
// Hàm registerRootComponent sẽ đăng ký component App làm component gốc (root).
registerRootComponent(App);
