import { Text, ScrollView } from 'react-native';

const CourseCompare = () => {

  return (
    <ScrollView>
      <Text>
        Login
      </Text>
    </ScrollView>
  );
};

export default CourseCompare;