import { Text, ScrollView } from 'react-native';

const LessonDetail = () => {

  return (
    <ScrollView>
      <Text>
        Login
      </Text>
    </ScrollView>
  );
};

export default LessonDetail;