import { Text, ScrollView } from 'react-native';

const CourseForm = () => {

    return (
        <ScrollView>
            <Text>
                CourseForm
            </Text>
        </ScrollView>
    );
};

export default CourseForm;