import { Text, ScrollView } from 'react-native';

const ApplyInstructor = () => {

    return (
        <ScrollView>
            <Text>
                ApplyInstructor
            </Text>
        </ScrollView>
    );
};

export default ApplyInstructor;