import { Text, ScrollView } from 'react-native';

const ChatListMain = () => {

  return (
    <ScrollView>
      <Text>
        ChatListMain
      </Text>
    </ScrollView>
  );
};

export default ChatListMain;