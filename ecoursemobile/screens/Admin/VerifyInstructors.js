import { Text, ScrollView } from 'react-native';

const StudentProgress = () => {

  return (
    <ScrollView>
      <Text>
        Login
      </Text>
    </ScrollView>
  );
};

export default StudentProgress;