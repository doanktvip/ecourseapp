import { Text, ScrollView } from 'react-native';

const StatsTab = () => {

    return (
        <ScrollView>
            <Text>
                StatsTab
            </Text>
        </ScrollView>
    );
};

export default StatsTab;